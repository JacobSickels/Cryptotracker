// Actions for the filters reducer

export const setStartDate = (startDate) => ({
    type: 'SET_START_DATE',
    startDate
});


export const setEndDate = (startDate) => ({
    type: 'SET_END_DATE',
    startDate
});